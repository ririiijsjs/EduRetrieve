specVersion: "v1"
serviceId: "my-react-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-react-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
